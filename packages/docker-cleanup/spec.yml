---
name: docker-cleanup
version: 0.0.2
license: ASL 2.0
iteration: 1
vendor: Asteris
architecture: noarch
type: rpm

targets:
  - src: '{{.SpecRoot}}/docker-cleanup'
    dest: /etc/cron.hourly/

scripts:
  after-install: |
    chmod a+x /etc/cron.hourly/docker-cleanup
