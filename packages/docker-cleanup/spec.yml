---
name: docker-cleanup
version: 0.0.1
license: ASL 2.0
iteration: 1
vendor: Asteris
architecture: noarch
type: rpm

depends:
  - cron
  - docker

targets:
  - src: '{{.SpecRoot}}/docker-cleanup'
    dest: /etc/cron.hourly/

scripts:
  after-install: |
    chmod +x /usr/bin/docker-cleanup
