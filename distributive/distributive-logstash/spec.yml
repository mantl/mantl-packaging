
name: distributive-logstash
version: 0.2.5
iteration: 1
license: ASL 2.0
description: Distributive-logstash is a configuration for running distributed health checks.
url: https://github.com/mantl/distributive
type: rpm

targets:
  - src: "{{.SpecRoot}}/distributive-logstash.json"
    dest: /etc/distributive.d/distributive-logstash.json
