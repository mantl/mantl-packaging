
name: distributive-consul
version: 0.2.4
iteration: 1
license: ASL 2.0
description: Distributive-consul is a configuration for distributed health checks.
url: https://github.com/CiscoCloud/distributive
type: rpm

targets:
  - src: "{{.SpecRoot}}/distributive-consul.json"
    dest: /etc/distributive.d/distributive-consul.json
