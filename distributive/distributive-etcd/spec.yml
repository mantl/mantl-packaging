
name: distributive-etcd
version: 0.2.1-test
iteration: 1
license: ASL 2.0
description: Distributive-etcd a configuration for distributed health checks.
url: https://github.com/CiscoCloud/distributive
type: rpm

targets:
  - src: "{{.SpecRoot}}/distributive-etcd.json"
    dest: /etc/distributive.d/distributive-etcd.json
